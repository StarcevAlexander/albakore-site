import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-installment-badge',
  imports: [RouterLink],
  templateUrl: './installment-badge.html',
  styleUrl: './installment-badge.scss',
})
export class InstallmentBadge {}
